# Substitute values or use: kubectl create secret generic stock-service-secret ...
# Do not commit real credentials. Example only.
apiVersion: v1
kind: Secret
metadata:
  name: stock-service-secret
  namespace: stock
type: Opaque
stringData:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://REPLACE_HOST:5432/REPLACE_DB"
  SPRING_DATASOURCE_USERNAME: "postgres"
  SPRING_DATASOURCE_PASSWORD: "REPLACE_PASSWORD"
  aws.access-key-id: "REPLACE_AWS_ACCESS_KEY"
  aws.secret-access-key: "REPLACE_AWS_SECRET_KEY"
  JWT_SECRET: "REPLACE_JWT_SECRET_BASE64"
